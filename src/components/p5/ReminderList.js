// @todo , this should use the ReminderList.module.css

const ReminderList = (props) => {
  return (  <ul>
    {/* @todo finish this component */}
    {/* if reminders are empty, show 'no reminders yet' */}
    {/* Otherwise: every reminder is in an <li> and that <li> has the 'reminder' class from the css module. */}

  </ul>)
}

export default ReminderList;
